name: pointer-proxy

on:
  push:
  workflow_call:

jobs:
  pointer-pipe-docker-image:
    uses: demo-hell/reusable-pipelines/.github/actions/actions-dockers/pipelines/pipe-docker-image.yml@main
    secrets: inherit